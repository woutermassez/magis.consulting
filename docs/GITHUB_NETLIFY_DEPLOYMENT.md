# GitHub + Netlify deployment

1. Pak deze ZIP uit.
2. Upload alle bestanden naar de root van je GitHub repository.
3. Vervang de oude `index.html` door deze versie.
4. Commit naar `main`.
5. Wacht op Netlify deploy.
6. Test `/sitemap.xml`, `/robots.txt`, `/llms.txt` en `/security.txt`.
